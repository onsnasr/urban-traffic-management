# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type UserType {
  id: Int!
  email: String!
  role: String!
}

type AuthResponse {
  token: String!
  user: UserType!
}

type VehicleType {
  id: Int!
  brand: String!
  model: String!
  licensePlate: String!
  driverName: String
  latitude: Float
  longitude: Float
}

type GpsPositionType {
  id: Int!
  latitude: Float!
  longitude: Float!
  vehicleId: Int!
  recordedAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type TrafficZoneType {
  id: Int!
  name: String!
  latitude: Float!
  longitude: Float!
  vehicleCount: Float!
  density: String!
}

type IncidentObject {
  id: Int!
  type: String!
  status: String!
  description: String!
  latitude: Float!
  longitude: Float!
}

type NotificationType {
  id: Int!
  message: String!
  isRead: Boolean!
  userId: Int
}

type Query {
  hello: String!
  vehicles: [VehicleType!]!
  vehicle(id: Int!): VehicleType!
  vehicleHistory(vehicleId: Int!): [GpsPositionType!]!
  trafficZones: [TrafficZoneType!]!
  trafficZone(id: Int!): TrafficZoneType!
  congestedZones: [TrafficZoneType!]!
  incidents: [IncidentObject!]!
  incident(id: Int!): IncidentObject!
  notifications: [NotificationType!]!
  unreadNotifications: [NotificationType!]!
}

type Mutation {
  register(input: RegisterInput!): UserType!
  login(email: String!, password: String!): AuthResponse!
  createVehicle(brand: String!, model: String!, licensePlate: String!, driverName: String): VehicleType!
  updateVehiclePosition(id: Int!, latitude: Float!, longitude: Float!): VehicleType!
  createTrafficZone(name: String!, latitude: Float!, longitude: Float!): TrafficZoneType!
  updateTrafficDensity(id: Int!, vehicleCount: Int!): TrafficZoneType!
  createIncident(type: String!, description: String!, latitude: Float!, longitude: Float!): IncidentObject!
  updateIncidentStatus(id: Int!, status: String!): IncidentObject!
  createNotification(message: String!, userId: Int): NotificationType!
  markNotificationAsRead(id: Int!): NotificationType!
}

input RegisterInput {
  email: String!
  password: String!
  role: String
}